<form method="post" action="{{ path('app_departments_employees_delete', {'id': departments_employee.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ departments_employee.id) }}">
    <button class="btn">Delete</button>
</form>
